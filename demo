/**
 * @param {any} obj
 * @param {any} classFunction
 * @return {boolean}
 */
var checkIfInstanceOf = function(obj, classFunction) {
    // 1. Check for invalid inputs (null or undefined)
    if (obj === null || obj === undefined || typeof classFunction !== 'function') {
        return false;
    }

    // 2. Get the prototype of the current object
    let currPrototype = Object.getPrototypeOf(obj);

    // 3. Traverse up the prototype chain
    while (currPrototype !== null) {
        if (currPrototype === classFunction.prototype) {
            return true;
        }
        currPrototype = Object.getPrototypeOf(currPrototype);
    }

    return false;
};
